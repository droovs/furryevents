# 📅 Календарь событий 2026

Статический сайт для отображения календаря событий на 2026 год. Построен на vanilla HTML/CSS/JS с использованием Tailwind CSS.

## ✨ Возможности

- **Годовой обзор** — все 12 месяцев на одном экране с мини-календарями
- **Детальный вид месяца** — полная сетка с событиями
- **Многодневные события** — отображаются как горизонтальные полосы
- **Фильтрация по категориям** — с сохранением в URL для шаринга
- **Тёмная тема** — автоматически или вручную
- **Адаптивный дизайн** — корректно работает на мобильных устройствах
- **Доступность** — семантическая разметка и навигация с клавиатуры

## 🚀 Запуск

Сайт полностью статический. Для локального запуска нужен любой HTTP-сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (npx)
npx serve

# PHP
php -S localhost:8000
```

Затем откройте http://localhost:8000 в браузере.

> ⚠️ Открытие `index.html` напрямую (file://) не будет работать из-за ограничений CORS при загрузке JSON.

## 🌐 Деплой на GitHub Pages

### Автоматический деплой

1. Создайте репозиторий на GitHub и загрузите файлы:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
   git push -u origin main
   ```

2. В настройках репозитория откройте **Settings** → **Pages**

3. В разделе **Source** выберите:
   - **Deploy from a branch**
   - Branch: `main`
   - Folder: `/ (root)`

4. Нажмите **Save**

5. Через 1-2 минуты сайт будет доступен по адресу:
   ```
   https://YOUR_USERNAME.github.io/YOUR_REPO/
   ```

### Важные файлы для GitHub Pages

| Файл | Описание |
|------|----------|
| `.nojekyll` | Отключает обработку Jekyll для ускорения деплоя |
| `404.html` | Обрабатывает SPA-маршрутизацию при прямом переходе по URL |

### Кастомный домен (опционально)

1. Создайте файл `CNAME` в корне проекта:
   ```
   your-domain.com
   ```

2. Настройте DNS у регистратора домена:
   - Для apex домена: A-записи на IP GitHub Pages
   - Для субдомена: CNAME-запись на `YOUR_USERNAME.github.io`

3. В **Settings** → **Pages** включите **Enforce HTTPS**

## 📁 Структура проекта

```
/
├── index.html      # Основная HTML-страница
├── style.css       # Дополнительные стили
├── app.js          # JavaScript логика
├── events.json     # Данные событий
├── 404.html        # Обработка SPA-маршрутизации для GitHub Pages
├── .nojekyll       # Отключение Jekyll на GitHub Pages
└── README.md       # Эта документация
```

## 📝 Формат данных events.json

```json
{
  "events": [
    {
      "id": "unique-id",
      "title": "Название события",
      "startDate": "2026-03-15",
      "endDate": "2026-03-18",
      "color": "#3b82f6",
      "category": "conference",
      "location": "Москва",
      "description": "Подробное описание события",
      "url": "https://example.com"
    }
  ],
  "categories": [
    { "id": "conference", "name": "Конференции", "color": "#3b82f6" }
  ]
}
```

### Поля события

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `id` | string | ✅ | Уникальный идентификатор события |
| `title` | string | ✅ | Название события |
| `startDate` | string | ✅ | Дата начала в формате `YYYY-MM-DD` |
| `endDate` | string | ✅ | Дата окончания в формате `YYYY-MM-DD` |
| `color` | string | ❌ | Цвет события (HEX), по умолчанию берётся из категории |
| `category` | string | ✅ | ID категории из списка `categories` |
| `location` | string | ✅ | Место проведения |
| `description` | string | ✅ | Описание события |
| `url` | string | ✅ | Ссылка на страницу события |

### Поля категории

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | string | Уникальный идентификатор категории |
| `name` | string | Отображаемое название |
| `color` | string | Цвет категории (HEX) |

## ➕ Как добавить новое событие

1. Откройте файл `events.json`
2. Добавьте новый объект в массив `events`:

```json
{
  "id": "my-new-event",
  "title": "Моё событие",
  "startDate": "2026-05-01",
  "endDate": "2026-05-03",
  "color": "#10b981",
  "category": "meetup",
  "location": "Москва",
  "description": "Описание моего события",
  "url": "https://example.com/my-event"
}
```

3. Сохраните файл и обновите страницу

### Добавление новой категории

Если нужна новая категория, добавьте её в массив `categories`:

```json
{
  "id": "my-category",
  "name": "Моя категория",
  "color": "#f59e0b"
}
```

## 🎨 Рекомендуемые цвета

| Цвет | HEX | Использование |
|------|-----|---------------|
| 🟣 Фиолетовый | `#8b5cf6` | Конвенции |
| 🟢 Зелёный | `#10b981` | Встречи |
| 🔴 Красный | `#ef4444` | Дедлайны |
| 🟡 Жёлтый | `#f59e0b` | Онлайн-события |
| 🔵 Голубой | `#06b6d4` | Мастер-классы |
| 🟣 Розовый | `#ec4899` | Вечеринки |

## ⌨️ Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `←` / `→` | Переключение месяцев (в режиме месяца) |
| `Escape` | Закрыть модальное окно / вернуться к году |
| `Enter` / `Space` | Открыть выбранный месяц |

## 🔗 URL-параметры

Состояние фильтров и текущий месяц сохраняются в URL:

```
?categories=convention,meetup&month=3
```

- `categories` — список активных категорий через запятую
- `month` — номер месяца (0-11)

## 🌙 Тёмная тема

Тема определяется автоматически по системным настройкам (`prefers-color-scheme`). Можно переключить вручную кнопкой в шапке — выбор сохраняется в `localStorage`.

## 📱 Адаптивность

- **Desktop** (>1024px): 4 колонки месяцев
- **Tablet** (>640px): 2-3 колонки
- **Mobile** (<640px): 1 колонка

## 🛠 Технологии

- HTML5 + семантическая разметка
- CSS3 + Tailwind CSS (CDN)
- Vanilla JavaScript (ES6+)
- Шрифты: [Outfit](https://fonts.google.com/specimen/Outfit) + [JetBrains Mono](https://fonts.google.com/specimen/JetBrains+Mono)

## 📄 Лицензия

MIT — используйте свободно для своих проектов.
